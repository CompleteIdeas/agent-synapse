# packages/memory — ARCHIVED

This package has been replaced by the standalone **AgentWorkingMemory (AWM)** project at:
`C:\Users\robert\Personal-Projects\AgentWorkingMemory`

AWM runs as an external service. Agents access it via MCP tools (memory_write, memory_recall, etc.)
or the HTTP API at http://127.0.0.1:8400.

Source code archived to: `archive/memory-src-archived/`
The memory-client wrapper is at: `packages/memory-client/`

To start AWM: `cd AgentWorkingMemory && npx tsx src/index.ts`
